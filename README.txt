class01:流式布局
    1. 自定义View的几个构造方法
    2. 
class05: AndroidUI滑动冲突
class06: 文字绘制  单位是像素
    1. baseline
    2. setTextAline(center) 的效果???? 为什么会对齐
    3. 奇奇怪怪的问题可能会超过ascent和dscenet (藏族文字) 会超过  但是不会超过top和bottom
    4. top为-值 bottom为正值  以baseline为基准
    5. 打印日志 看中文的top与ascent的值是不是一样的
    5. 属性动画为什么是percent而不是mPercent
    作业
    1. 自己实现仿今日头条的文字渐变的viewpager指示器

class07:锦鲤动画