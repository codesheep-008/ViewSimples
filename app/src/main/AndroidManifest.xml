<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codesheep008.viewsimples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ViewSimples">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".class05.clickevent.ClickEventActivity" />
        <activity android:name=".class05.dispatch.DispatchActivity" />

        <activity android:name=".class06.TextViewColorTestActivity" />
        <activity android:name=".class01.FlowLayoutActivity"/>
    </application>

</manifest>